# 📊 User Tracking & Analytics System

Комплексная система отслеживания поведения пользователей для улучшения поиска и персонализации рекомендаций.

## 🎯 Обзор

Система включает 4 основных компонента:
1. **UserBehaviorProfile** - долгосрочный профиль предпочтений пользователя
2. **ConversationAnalytics** - аналитика по каждой сессии
3. **ProductInteraction** - отслеживание взаимодействий с товарами
4. **MessageAnalysis** - интеллектуальный анализ сообщений и намерений

## 📦 Структура базы данных

### user_behavior_profiles
Долгосрочный профиль пользователя с обучением из всех сессий:
- `category_preferences` (JSONB) - предпочитаемые категории с весами
- `price_ranges` (JSONB) - ценовые диапазоны по категориям
- `brand_preferences` (JSONB) - предпочитаемые бренды
- `communication_style` - стиль общения (brief/balanced/detailed)
- `avg_session_duration` - средняя длительность сессии
- `success_rate` - процент успешных сессий
- `common_keywords` (JSONB) - часто используемые ключевые слова

### conversation_analytics
Аналитика по каждой сессии:
- `message_count`, `search_count` - счетчики активности
- `products_shown`, `products_clicked` - метрики продуктов
- `primary_intent` - основное намерение (exploration/purchase/comparison/information)
- `overall_sentiment` - общий тон разговора (positive/neutral/negative)
- `key_topics` (JSONB) - ключевые темы разговора
- `categories_explored` (JSONB) - изученные категории
- `flow_quality_score` - качество потока разговора (0-1)
- `extracted_preferences` (JSONB) - извлеченные предпочтения

### product_interactions
Отслеживание каждого взаимодействия с товаром:
- `interaction_type` - тип (viewed/clicked/compared/dismissed)
- `product_name`, `product_price`, `product_category`, `product_brand`
- `view_duration_seconds` - длительность просмотра
- `implicit_score` - неявная оценка интереса (0-1)
- `position_in_results` - позиция в результатах поиска
- `search_query`, `search_type` - контекст поиска

## 🚀 API Endpoints

### Аутентифицированные (требуется JWT):

#### GET /api/analytics/profile
Получить профиль поведения пользователя
```json
{
  "communication_style": "balanced",
  "total_sessions": 15,
  "avg_session_duration_mins": 5.2,
  "success_rate": 73.3,
  "top_categories": ["electronics", "clothing"],
  "top_brands": ["Apple", "Samsung"],
  "common_keywords": ["gaming", "laptop", "wireless"]
}
```

#### GET /api/analytics/recommendations
Персонализированные рекомендации
```json
{
  "preferred_categories": ["electronics", "sports"],
  "preferred_brands": ["Nike", "Sony"],
  "communication_style": "detailed",
  "interaction_stats": {
    "total_interactions": 50,
    "engagement_rate": 0.68
  }
}
```

#### GET /api/analytics/summary
Сводная аналитика по всем сессиям
```json
{
  "total_sessions": 20,
  "avg_message_count": 8.5,
  "success_rate": 0.75,
  "sentiment_distribution": {
    "positive": 12,
    "neutral": 6,
    "negative": 2
  },
  "intent_distribution": {
    "purchase": 10,
    "exploration": 7,
    "comparison": 3
  }
}
```

#### GET /api/analytics/interactions
Статистика взаимодействий с товарами
```json
{
  "total_interactions": 120,
  "views": 80,
  "clicks": 30,
  "engagement_rate": 0.375,
  "avg_implicit_score": 0.45
}
```

### Публичные (без аутентификации):

#### GET /api/analytics/session/:sessionId
Инсайты по конкретной сессии
```json
{
  "session_id": "...",
  "message_count": 10,
  "products_shown": 5,
  "products_clicked": 2,
  "primary_intent": "purchase",
  "sentiment": "positive",
  "flow_quality_score": 0.85,
  "found_product": true
}
```

#### POST /api/analytics/track/click
Отследить клик по товару
```json
{
  "session_id": "...",
  "product_id": "..."
}
```

#### POST /api/analytics/finalize/:sessionId
Завершить сессию и запустить обучение

## 🧠 Сервисы и алгоритмы

### UserBehaviorService
**Основные методы:**
- `LearnFromSession()` - обучение из завершенной сессии
- `GetRecommendedCategories()` - топ-N рекомендуемых категорий
- `GetPreferredBrands()` - топ-N предпочитаемых брендов
- `GetPriceRange()` - типичный ценовой диапазон по категории

**Алгоритм обучения:**
1. Анализ категорий: увеличение веса на 0.1 за каждое исследование
2. Анализ брендов: подсчет частоты с весами (клик = 3, сравнение = 2, просмотр = 1)
3. Анализ цен: расширение диапазона на основе просмотренных товаров
4. Определение стиля: на основе среднего количества слов в сообщениях
5. Расчет метрик: успешность, длительность, количество сообщений

### ConversationAnalyticsService
**Основные методы:**
- `StartSession()` - инициализация аналитики для новой сессии
- `UpdateSessionMetrics()` - обновление метрик в реальном времени
- `EndSession()` - финализация с генерацией инсайтов
- `AddCategory()`, `AddBrandMention()`, `AddPriceMention()` - отслеживание упоминаний

**Анализ:**
- Извлечение ключевых тем через эмбеддинги
- Анализ тональности на основе словаря
- Определение намерений (exploration/purchase/comparison/information)
- Расчет качества потока разговора

### ProductInteractionService
**Основные методы:**
- `TrackProductView()` - отслеживание просмотра товара
- `TrackProductClick()` - отслеживание клика
- `TrackProductComparison()` - добавление в сравнение
- `TrackProductDismissal()` - отклонение товара
- `GetTopProductsForUser()` - лучшие товары на основе implicit score

**Implicit Score:**
- Клик: 0.7
- Сравнение: 0.5
- Длительный просмотр (>30 сек): 0.4
- Просмотр (10-30 сек): 0.2
- Короткий просмотр: 0.1
- Отклонение: -0.3

### MessageAnalysisService
**Основные методы:**
- `DetectIntent()` - определение намерения с уверенностью
- `ExtractPrices()` - извлечение цен из текста
- `ExtractBrands()` - извлечение упоминаний брендов
- `AnalyzeMessageSentiment()` - анализ тональности
- `ExtractProductRequirements()` - извлечение требований к товару

**Типы намерений:**
- `exploration` - поиск, исследование
- `purchase` - намерение купить
- `comparison` - сравнение товаров
- `information` - запрос информации
- `clarification` - уточнение
- `specification` - технические характеристики
- `budget_inquiry` - вопрос о цене/бюджете

## 🔄 Интеграция в WebSocket

### TrackingMiddleware
Middleware для автоматического отслеживания:

```go
// При получении сообщения пользователя
tracking.TrackMessageAnalysis(ctx, sessionID, userID, message)

// При показе товаров
tracking.TrackProductViews(ctx, sessionID, userID, products, searchQuery, searchType, messagePosition)

// При выполнении поиска
tracking.TrackSearchQuery(ctx, sessionID, searchPhrase, searchType, category)

// При инициализации сессии
tracking.InitializeSessionAnalytics(ctx, sessionID, userID)

// При завершении сессии
tracking.FinalizeSession(ctx, sessionID, userID) // Запускает обучение
```

## 📈 Использование данных

### Улучшение поиска
1. **Персонализация категорий**: приоритет категорий с высоким весом
2. **Фильтрация брендов**: предложение предпочитаемых брендов
3. **Ценовые рекомендации**: товары в типичном ценовом диапазоне
4. **Стиль ответов**: краткие/подробные ответы на основе стиля общения

### Улучшение ответов агента
1. **Определение намерений**: адаптация ответов под тип запроса
2. **Контекст предпочтений**: использование предыдущих выборов
3. **Оптимизация диалога**: сокращение количества уточнений
4. **Качество потока**: мониторинг и улучшение качества разговора

### Аналитика продуктов
1. **Популярность товаров**: счетчики просмотров/кликов
2. **Конверсия**: процент кликов от просмотров
3. **Позиционирование**: влияние позиции на кликабельность
4. **A/B тестирование**: сравнение результатов разных подходов

## 🛠 Миграции

Последовательно выполнить миграции:
```bash
psql -U postgres -d mylittleprice -f backend/migrations/011_add_user_behavior_profile.sql
psql -U postgres -d mylittleprice -f backend/migrations/012_add_conversation_analytics.sql
psql -U postgres -d mylittleprice -f backend/migrations/013_add_product_interaction.sql
```

## 📊 Примеры запросов

### Получить профиль пользователя
```bash
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/analytics/profile
```

### Отследить клик по товару
```bash
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"session_id":"...","product_id":"..."}' \
  http://localhost:8080/api/analytics/track/click
```

### Завершить сессию и запустить обучение
```bash
curl -X POST \
  -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/analytics/finalize/SESSION_ID
```

## 🔐 Безопасность и приватность

- Все персональные данные привязаны к UUID пользователя
- Анонимные пользователи также отслеживаются (без user_id)
- Данные используются только для улучшения опыта
- Возможность полного удаления данных по запросу (GDPR)

## 🎯 Метрики успеха

Ключевые метрики для оценки эффективности:
- **Success Rate**: процент сессий, где пользователь нашел товар
- **Engagement Rate**: процент товаров, с которыми взаимодействовали
- **Flow Quality**: качество потока разговора (0-1)
- **Avg Session Duration**: средняя длительность сессии
- **Click-Through Rate**: процент кликов от показов

## 🚀 Будущие улучшения

1. **ML-модели**: обучение моделей на собранных данных
2. **Real-time персонализация**: динамическая адаптация в процессе разговора
3. **Collaborative filtering**: рекомендации на основе похожих пользователей
4. **Sentiment-based optimization**: адаптация стиля на основе настроения
5. **A/B тестирование**: автоматизированное тестирование подходов
